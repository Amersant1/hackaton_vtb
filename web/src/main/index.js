import React from 'react';
import ReactDOM from "react-dom/client";

import AppPage from '~/main/page/AppPage.js';

const root = ReactDOM.createRoot(document.getElementById("myPage"));
root.render(
   <AppPage/>,
);   
